# Hope

Some small patches for [Hope](https://en.wikipedia.org/wiki/Hope_(programming_language)) to make it work on my machine(s) (with main motivation to teach @vmxdev some functional programming).

You can build it with `make` and run REPL with `make run`.
